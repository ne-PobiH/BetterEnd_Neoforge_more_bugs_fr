// BetterX libraries are embedded into the main source set as AiBlib.
// External composite builds are intentionally disabled to avoid depending on
// BCLib, WunderLib or WorldWeaver at build/runtime.
println "Using embedded AiBlib library sources"
